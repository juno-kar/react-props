import { createTheme } from "@mui/material/styles";

import "@fontsource/josefin-sans";
import "@fontsource/indie-flower";

const theme = createTheme({
    typography: {
      fontFamily: "'Josefin Sans', 'Indie Flower', sans-serif, cursive",
    },
  });

export { theme };
  
